# 시트별 분류 기준 (ISO/IEC 25010 품질 특성 매핑)

TC를 5개 시트(명세기반, 예외사항, 유즈케이스, 코드영향도, Regression)에 분류하기 위한 명시적 기준입니다.
프로젝트 불문 재사용 가능한 프레임워크 규칙입니다.

---

## 1. 명세기반

**정의**: 명세(기획서/디자인/요구사항)에 정의된 동작이 올바른지 검증 + **사용자가 화면 정보를 이해하고 조작할 수 있는지** 검증

| ISO/IEC 25010 품질 특성 | 범위 |
|------------------------|------|
| 기능 적합성: 완전성 | 명세에 정의된 기능이 모두 구현되었는가 |
| 기능 적합성: 정확성 | 명세에 정의된 동작이 정확하게 수행되는가 |
| 기능 적합성: 적절성 | 사용자 과업 달성에 필요한 기능이 제공되는가 |
| 사용성: 학습성 | 첫 사용자가 안내 문구/질문팁으로 기능을 이해할 수 있는가 |
| 사용성: 조작성 | 버튼 반응, 스크롤 동작, 키보드 인터랙션이 기대대로인가 |
| 사용성: 사용자 오류 방지 | 인라인 검증, CTA 비활성화 등으로 실수를 방지하는가 |
| 사용성: 접근성 | 스크린 리더, 색상 대비, 포커스 관리가 적절한가 |

**분류 규칙**: 단일 기능의 명세 기반 검증. 기대결과가 **정상 동작**인 경우.

---

## 2. 예외사항

**정의**: 비정상/무효/예상 외 조건에서의 시스템 동작 검증 + **보안 경계** 검증

| ISO/IEC 25010 품질 특성 | 범위 |
|------------------------|------|
| 신뢰성: 결함 허용성 | 무효 입력, 네트워크 오류 시 시스템이 안정적으로 동작하는가 |
| 신뢰성: 복구성 | 장애 후 정상 상태로 복구할 수 있는가 |
| 신뢰성: 성숙성 | 지속 사용 시 크래시/오류 없이 안정적인가 |
| 보안: 기밀성 | 개인정보 마스킹, 토큰 노출 방지가 적절한가 |
| 보안: 무결성 | XSS/인젝션 등 악의적 입력이 차단되는가 |
| 보안: 인증 | 세션 토큰 만료 처리, CSRF 방어가 작동하는가 |

**분류 규칙**: 기대결과가 **에러 메시지, 토스트, 검증 경고, 또는 우아한 저하(graceful degradation)**인 경우.

**경계 규칙**: 같은 입력(예: 50MB 첨부)이라도 → 49.9MB 성공=명세기반, 50.1MB 에러=예외사항. **기대결과**로 분류.

---

## 3. 유즈케이스

**정의**: 여러 상태/화면을 거치는 **완결된 사용자 여정** 검증 + **사용 품질(Quality in Use)** 검증

| ISO/IEC 25010 품질 특성 | 범위 |
|------------------------|------|
| 기능 적합성 (E2E) | 다단계 흐름의 최종 결과가 정확한가 |
| 사용 품질: 유효성 | 사용자가 목표를 정확하고 완전하게 달성하는가 |
| 사용 품질: 효율성 | 합리적인 단계/시간 내에 목표를 달성하는가 |
| 사용 품질: 만족도 | 각 단계에서 적절한 피드백이 제공되는가 |

**분류 규칙**: **3개 이상 상태 전이** 또는 **2개 이상 화면/컨텍스트**를 걸치는 사용자 여정.

**명세기반과의 경계**: A→B 단일 전이 = 명세기반, A→B→C→D 연속 여정 = 유즈케이스.

---

## 4. 코드영향도

**정의**: 코드 내부 지식이 필요한 **구현 수준 검증** + **성능 효율성** 검증

| ISO/IEC 25010 품질 특성 | 범위 |
|------------------------|------|
| 호환성: 상호운용성 | 외부 시스템(API, SDK, 서드파티) 연동이 정확한가 |
| 신뢰성 (기술적) | OOM, fire-and-forget 등 구현 수준의 안정성 |
| 성능 효율성: 시간 동작 | API 응답 시간, UI 렌더링 지연이 허용 범위 내인가 |
| 성능 효율성: 자원 활용 | 메모리 사용량, WebView 생명주기 관리가 적절한가 |
| 성능 효율성: 용량 | 동시 세션, 데이터 볼륨 한계가 적절한가 |

**분류 규칙**: TC 구성 또는 검증에 **내부 코드 지식**(API 필드명, 데이터 파이프라인, 코드 생명주기, 성능 측정)이 필요한 경우.

---

## 5. Regression

**정의**: 현재 프로젝트 범위 **밖** 기존 기능의 정상 동작 유지 검증 + **이식성** 검증

| ISO/IEC 25010 품질 특성 | 범위 |
|------------------------|------|
| 기능 적합성 (기존 기능) | 기존 기능이 변경 없이 정상 동작하는가 |
| 이식성: 적응성 | iOS/Android/Web/MO Web 간 일관된 동작인가 |
| 호환성: 공존성 | 신규 기능과 기존 기능이 충돌 없이 공존하는가 |
| 사용 품질: 컨텍스트 커버리지 | 언어, 지역, 디바이스 변경 시 기존 동작이 유지되는가 |

**분류 규칙**: 현재 프로젝트 스코프에 **정의되지 않은** 기존 기능이 영향받지 않는지.

---

## 분류 의사결정 플로차트

```
TC 분류 시작
│
├─ Q1. 현재 프로젝트 범위 밖 기존 기능 검증인가?
│   → YES → Regression
│
├─ Q2. TC 구성/검증에 내부 코드 지식이 필요한가?
│   (API 필드명, 데이터 파이프라인, 성능 측정 등)
│   → YES → 코드영향도
│
├─ Q3. 기대결과가 에러/경고/보안위반/우아한 저하인가?
│   → YES → 예외사항
│
├─ Q4. 3+상태 전이 또는 2+화면 걸치는 사용자 여정인가?
│   → YES → 유즈케이스
│
└─ 위 모두 아님 → 명세기반
```

**플로차트 사용 규칙**:
- Q1→Q2→Q3→Q4 순서대로 판단하며, 첫 번째 YES에서 분류 확정
- 어떤 TC든 정확히 1개 시트에만 도달해야 함 (중복 배치 금지)
- 판단이 모호한 경우 기대결과의 성격(정상 vs 비정상)을 우선 기준으로 삼음

**플로차트 보조 규칙 1: 플랫폼 분리 예외 (코드 구현 차이)**
- 코드 분석에서 플랫폼별 구현 차이가 식별된 기능은 Q1(Regression) 예외 처리
- 해당 TC는 Q2~Q5 판단을 거쳐 **기능 시트**(명세기반/예외사항/유즈케이스/코드영향도)에 배치
- 적용 조건: `/매트릭스` Step 4-P 태깅이 존재하는 경우에만
- 차이 없는 기능의 플랫폼 분리는 기존 규칙대로 Regression에서만 수행

**플로차트 보조 규칙 2: 플랫폼 UI 강제 분리 (UI 구성 차이)**
- PC와 Mobile 간 **UI 구성이 다른 기능**은 플랫폼별 별도 TC를 해당 기능 시트에 배치
- UI 차이 유형: 노출 요소 차이, 레이아웃 차이, 인터랙션 방식 차이, 네비게이션 차이, 한쪽 전용 기능
- 적용 조건: `/매트릭스` Step 4-PUI 태깅이 존재하는 경우
- 사전조건에 `Only PC` 또는 `Only Mobile` 명시
- 상세 기준: `기준_실행단위분해.md` A3 규칙 참조

---

## ISO/IEC 25010 품질 특성 커버리지 맵

아래 7개 특성(Maintainability 제외)이 최소 1개 시트에 할당되었는지 확인합니다.

| ISO/IEC 25010 품질 특성 | 할당 시트 | 비고 |
|------------------------|----------|------|
| 기능 적합성 (Functional Suitability) | 명세기반, 유즈케이스, Regression | 3개 시트에서 다른 관점으로 검증 |
| 성능 효율성 (Performance Efficiency) | 코드영향도 | 시간 동작, 자원 활용, 용량 |
| 호환성 (Compatibility) | 코드영향도, Regression | 상호운용성(코드), 공존성(Regression) |
| 사용성 (Usability) | 명세기반 | 학습성, 조작성, 오류 방지, 접근성 |
| 신뢰성 (Reliability) | 예외사항, 코드영향도 | 결함 허용성(예외), 기술적 안정성(코드) |
| 보안 (Security) | 예외사항 | 기밀성, 무결성, 인증 |
| 이식성 (Portability) | Regression | 적응성 (플랫폼 간 일관성) |

> **Maintainability(유지보수성)**은 코드 리뷰/정적 분석 영역이므로 QA TC 시트에서 제외합니다.

---

## 비고란 포맷

TC 비고(備考)란에 ISTQB 기법과 품질 특성을 함께 표기합니다.

```
{ISTQB 기법} │ {품질특성}:{하위특성}
```

**예시**:
- `동치분할 │ 기능적합성:정확성`
- `에러추측 │ 신뢰성:결함허용성`
- `상태전이 │ 사용품질:유효성`
- `데이터흐름 │ 성능효율성:시간동작`
- `리스크기반 │ 이식성:적응성`

8개 컬럼 구조는 유지하며, 기존 비고란 형식을 확장합니다.
