# 유저스토리 기반 TC 도출 기준

행동 기반 기능 테스트 관점에서, 사용자의 실제 조건 조합에 따른 시스템 동작을 검증하는 유저스토리 TC의 도출 기준입니다.
프로젝트 불문 재사용 가능한 프레임워크 규칙입니다.

---

## 1. 정의와 목적

### 유저스토리 TC란

**형식**: `[사용자 상태]에서 [사용자 상황]일 때 [사용자 행동]을 하면 → [기대되는 시스템 동작]`

**본질**: 행동 기반 기능 테스트. 명세에 정의된 기능이 **다양한 사용자 조건 조합**에서 올바르게 동작하는지 검증합니다.

### 기존 TC와의 관계

| 유형 | 검증 질문 | 조건 설정 | 시트 |
|------|----------|----------|------|
| 명세기반 TC | "이 기능이 동작하는가?" | 단일 조건 | 명세기반 |
| E2E TC | "이 시스템 경로가 연결되는가?" | 제네릭 (기본 조건) | 유즈케이스 |
| **유저스토리 TC** | **"이 조건 조합에서 기능이 올바르게 동작하는가?"** | **특정 상태+상황 조합** | **유즈케이스** |

유저스토리 TC는 유즈케이스 시트에 배치됩니다. E2E TC와 함께 유즈케이스 시트를 구성하되, 서로 다른 관점에서 검증합니다.

### ISO/IEC 25010 매핑

| 품질 특성 | 검증 관점 |
|-----------|----------|
| 기능적합성:적절성 | 사용자 상태/상황에 적절한 기능을 제공하는가 |
| 사용품질:유효성 | 해당 조건에서 사용자가 과업을 완수할 수 있는가 |

---

## 2. 3축 분류 체계

### 2-1. 사용자 상태 (State) — "누가"

사용자의 **시스템 내 조건**. 행동 시점에 이미 결정되어 있는 조건.

| 차원 | 설명 | 프로젝트별 값 채우기 |
|------|------|---------------------|
| 인증 상태 | 로그인/비로그인, 세션 유효/만료 | |
| 사용자 유형 | 시스템이 구분하는 역할/권한 | |
| 이력 조건 | 과거 행동/데이터 존재 여부 | |
| 세션 상태 | 이전 세션의 진행 상태, 중단 지점 | |

**추출 소스**: Confluence (분기 조건), Code (조건 분기), Jira (사용자 유형 정의)

### 2-2. 사용자 상황 (Situation) — "어떤 맥락에서"

행동 시점의 **환경적·맥락적 조건**. UI 상태, 진입 경로, 디바이스 조건 등.

| 차원 | 설명 | 프로젝트별 값 채우기 |
|------|------|---------------------|
| 진입 경로 | 어떤 화면/경로를 통해 기능에 도달했는가 | |
| 화면 상태 | 스크롤 위치, 모달 열림/닫힘, 로딩 중 등 | |
| 환경 조건 | 네트워크, 디바이스, 플랫폼, OS 버전 | |
| 시간 조건 | 첫 진입, 재진입, 세션 타임아웃 직전 등 | |

**추출 소스**: Figma (화면 상태, 진입 경로, UI 인터랙션), Code (환경 분기)

### 2-3. 사용자 행동 (Action) — "무엇을 하는가"

사용자가 수행하는 **물리적 인터랙션**. 단일 행동이 아닌, 2개 이상의 행동 시퀀스로 구성됩니다.

| 차원 | 설명 | 프로젝트별 값 채우기 |
|------|------|---------------------|
| 직접 행동 | 탭, 클릭, 입력, 제출, 스와이프 | |
| 간접 행동 | 스크롤, 대기, 앱 전환, 뒤로가기 | |
| 중단/이탈 | 도중 이탈, 브라우저 닫기, 앱 킬 | |
| 전환 행동 | 화면 전환, 로그인/로그아웃, 플랫폼 이동 | |

**추출 소스**: Figma (인터랙션 정의), Confluence (사용자 흐름)

---

## 3. 도출 규칙

### 3-1. 추가 소스 불필요

유저스토리의 3축 값은 **기존 Phase 1 소스 분석 결과에서 추출**합니다. 새로운 소스를 수집할 필요 없이, 기존 요약 파일의 분기 조건·화면 상태·인터랙션 정의를 3축 관점으로 재분류합니다.

```
기존 소스 분석 결과
├─ 분기 조건 열거 → 사용자 상태 (State) 축으로 분류
├─ 화면 상태/진입 경로 → 사용자 상황 (Situation) 축으로 분류
└─ 인터랙션/사용자 흐름 → 사용자 행동 (Action) 축으로 분류
```

### 3-2. 조합 규칙 (조합 폭발 방지)

모든 State × Situation × Action 전수 조합은 생성하지 않습니다. 다음 4개 규칙으로 유의미한 조합만 선택합니다.

**규칙 1. 동작 차별성 기준**
상태/상황이 달라졌을 때 **시스템 동작이 실제로 달라지는** 조합만 선택합니다.

```
질문: "이 상태/상황 값이 바뀌면 기대결과가 달라지는가?"
→ YES: 조합에 포함
→ NO: 조합에서 제외 (다른 TC에서 이미 검증됨)
```

**규칙 2. Each-choice 최소 커버**
3축의 각 값이 **최소 1개** 유저스토리에 포함되어야 합니다. 규칙 1로 축소한 후에도 특정 값이 어느 조합에도 등장하지 않으면, 해당 값을 포함하는 조합을 추가합니다.

**규칙 2-b. 유저스토리 조합 하한선**
최소 유저스토리 수 = max(7, ceil(3축 전체 값 수 / 3))
- 예: 상태14 + 상황10 + 행동12 = 36 → ceil(36/3) = 12개
- 하한선 미달 시 리스크 상위 조합을 추가하여 하한선 충족

**규칙 2-c. Pairwise 보완 (상위 리스크 차원)**
리스크 최상위 3개 차원 간 모든 2-way 쌍이 최소 1개 유저스토리에 등장해야 한다.
- 리스크 최상위 차원: 기대결과가 달라지는 빈도가 높은 상위 3개 차원 선정
- 미커버 쌍 존재 시 해당 쌍을 포함하는 유저스토리 추가

**규칙 2-d. 제외 근거 테이블**
제외된 모든 조합의 근거를 기록:
| 제외 조합 (상태×상황×행동) | 제외 근거 | 대신 커버하는 TC |
|---------------------------|----------|-----------------|

**규칙 3. 행동 시퀀스 우선**
단일 행동(탭 1회)이 아닌, **2개 이상 행동이 시퀀스로 이어지는** 조합을 우선합니다. 이것이 유저스토리 TC가 명세기반 TC와 구별되는 핵심 조건입니다.

**규칙 4. 교차 분석 연동**
소스 간 교차 분석에서 불일치가 발견된 조건 조합은 **반드시 포함**합니다.

### 3-3. 조합 → TC 변환

각 유의미한 조합이 1개 TC가 됩니다.

| TC 컬럼 | 3축 매핑 |
|---------|---------|
| 사전조건 | 사용자 상태 (State) + 사용자 상황 (Situation) |
| 실행절차 | 사용자 행동 시퀀스 (Action) |
| 기대결과 | 해당 조건 조합에서의 올바른 시스템 동작 **(소스 원문 기반, R1/R3 적용)** |

---

## 4. 겹침 방지 규칙

### 4-1. 명세기반과의 경계

| 판별 기준 | 명세기반 | 유저스토리 |
|-----------|---------|-----------|
| 사전조건 복합도 | 단일 조건 또는 직교적 조건 | 상태+상황 복합 조건 |
| 행동 범위 | 단일 기능의 단일 행동 | **2+ 행동의 시퀀스** |
| 검증 관점 | 기능 자체의 정/오 | 조건 조합에서의 기능 정/오 |

**판별 규칙**: 사전조건에 사용자 상태+상황이 복합적으로 설정되고, 실행절차가 2개 이상 행동 시퀀스이면 → 유저스토리 (유즈케이스 시트)

### 4-2. 기존 E2E TC와의 경계

| 판별 기준 | E2E TC | 유저스토리 TC |
|-----------|--------|-------------|
| 관점 | 시스템 경로 연결성 | 사용자 행동 맥락 |
| 사전조건 | 제네릭 (기본 조건) | 특정 상태+상황 조합 |
| 동일 경로 허용 | 경로당 1개 | **같은 경로라도 조건 조합이 다르면 별도 TC** |

**판별 규칙**: 동일 경로(A→B→C)라도 사전조건이 "기본 상태"이면 E2E TC, "특정 상태+상황 조합"이면 유저스토리 TC

### 4-3. 예외사항과의 경계

유저스토리 TC의 기대결과는 **정상 동작**입니다.
기대결과가 에러/경고/보안위반/우아한 저하이면, `기준_시트분류.md`의 Q3에 의해 **예외사항 시트**로 분류됩니다.

### 4-4. 코드영향도와의 경계

유저스토리 TC의 검증에 내부 코드 지식이 필요하면, `기준_시트분류.md`의 Q2에 의해 **코드영향도 시트**로 분류됩니다.
유저스토리 TC는 **사용자가 관찰 가능한 동작**만 검증합니다.

---

## 5. 기준_시트분류.md 플로차트와의 관계

유저스토리 TC는 기존 분류 플로차트를 **그대로 따릅니다**.

```
TC 분류 시작
│
├─ Q1. 프로젝트 범위 밖 기존 기능? → Regression
├─ Q2. 내부 코드 지식 필요?       → 코드영향도
├─ Q3. 기대결과가 에러/경고?       → 예외사항
├─ Q4. 2+ 행동 시퀀스 여정?       → 유즈케이스 ← 유저스토리 TC는 여기
└─ 위 모두 아님                    → 명세기반
```

유저스토리 TC는 3축 조합에서 도출되므로 **자연스럽게 Q4 YES**(2+ 행동 시퀀스)에 해당합니다. 별도의 플로차트 분기를 추가할 필요 없습니다.

---

## 6. TC 작성 규칙

### 6-1. 비고란 형식

```
시나리오테스팅 │ 기능적합성:적절성
```
또는
```
시나리오테스팅 │ 사용품질:유효성
```

- **기능적합성:적절성** — 사용자 상태/상황에 따라 시스템이 적절한 기능을 제공하는지 검증할 때
- **사용품질:유효성** — 해당 조건에서 사용자가 과업을 완수할 수 있는지 검증할 때

### 6-2. 테스트제목 형식

```
[상태 요약] [상황 요약]에서 [핵심 행동 시퀀스]
```

상태와 상황이 사전조건에서 드러나므로, 테스트제목에는 핵심 정보만 포함합니다.

### 6-3. ISTQB 기법 매핑

| 기법 | 적용 조건 |
|------|----------|
| **시나리오테스팅** (주 기법) | 모든 유저스토리 TC에 기본 적용 |
| 조건기반 (보조) | 상태+상황 조합 조건이 기대결과를 결정할 때 |
| 상태전이 (보조) | 행동 시퀀스에 명시적 상태 변화가 포함될 때 |

### 6-4. 정확성 보장 (R1-R5 적용)

유저스토리 TC에도 기존 정확성 보장 규칙이 동일하게 적용됩니다:

- **R1**: 기대결과는 소스 파일을 Read로 열어 원문 확인 후 작성
- **R3**: 시스템 동작 기술 시 소스 원문 표현 유지
- **R4**: 상태/상황 값이 "N종"으로 명시된 경우 전수 나열 확인
- **R5**: 2+ 하위 조건이 있으면 각각 별도 TC로 전개

---

## 7. /매트릭스 통합 지점

Phase 1.5 매트릭스 생성 시, **기존 Step 4 "테스트 차원 열거" 이후에 추가 단계를 삽입**합니다.

### Step 4-US. 유저스토리 차원 열거

```
기존 Step 4 (입력값/분기 조건/상태 전이 열거)
    ↓
Step 4-US. 유저스토리 차원 열거 (신규)
    1. 기존 차원에서 사용자 상태/상황/행동을 3축으로 분리 추출
    2. 3축 테이블에 프로젝트별 값 채우기
    3. 조합 규칙(3-2)에 따라 유의미한 조합 생성
    4. 조합 테이블에 추가: "대상 시트: 유즈케이스, ISTQB 기법: 시나리오테스팅"
    ↓
기존 Step 5 (조합 테이블 생성) — 유저스토리 조합이 포함된 상태
```

### 매트릭스 출력 형식 (영역별 추가 섹션)

기존 영역별 섹션에 다음을 추가합니다:

```markdown
### 유저스토리 차원 (이 영역)

#### 사용자 상태
| 차원 | 값 목록 |
|------|--------|

#### 사용자 상황
| 차원 | 값 목록 |
|------|--------|

#### 사용자 행동
| 차원 | 값 목록 |
|------|--------|

#### 유저스토리 조합
| US-ID | 상태 | 상황 | 행동 시퀀스 | 동작 차별성 근거 |
|-------|------|------|-----------|----------------|
```

---

## 8. /TC생성 통합 지점

유즈케이스 시트 생성 프로토콜에서 기존 E2E TC 생성 후 유저스토리 TC를 추가 생성합니다.

```
유즈케이스 시트 생성 프로토콜
    1. 매트릭스 매핑 추출 (기존)
    2. E2E TC 생성 (기존 프로토콜 유지)
    3. 유저스토리 TC 생성 (신규)
       - 매트릭스의 유저스토리 조합(US-ID)을 순회
       - 각 조합을 TC로 변환 (3-3 규칙 적용)
       - 겹침 방지 규칙(4장)으로 기존 E2E TC와 중복 확인
    4. 자기 점검
       - 3축 각 값이 최소 1개 TC에 포함되었는지 (Each-choice)
       - 교차 분석 발견 조건이 유저스토리에 반영되었는지
```

---

## 9. 자기 점검 체크리스트

유저스토리 TC 생성 완료 후 반드시 점검합니다:

- [ ] 3축의 모든 값이 최소 1개 TC에 포함되었는가? (Each-choice)
- [ ] 각 유저스토리 TC의 실행절차가 2+ 행동 시퀀스인가? (명세기반과의 경계)
- [ ] 각 유저스토리 TC의 기대결과가 정상 동작인가? (예외사항과의 경계)
- [ ] 기존 E2E TC와 동일 경로 + 동일 조건인 TC가 없는가? (중복 방지)
- [ ] 기대결과가 소스 원문에 근거하는가? (R1/R3)
- [ ] 교차 분석 발견 항목 중 행동 조합과 관련된 것이 반영되었는가?
- [ ] 동작 차별성 근거가 없는 조합이 포함되지 않았는가? (불필요 조합 방지)
