# QA 에이전트팀 아키텍처 설계 (v4.0)

> **목적**: 와디즈 서비스 도메인별 전문 에이전트팀을 구성하여 TC 수량과 깊이를 극대화
> **기반**: v3.0 프레임워크 (3-Phase 워크플로우, R1~R6, A1~A5)
> **설계일**: 2026-02-25

---

## 1. 아키텍처 개요

### 1.1 핵심 원칙

| 원칙 | 설명 |
|------|------|
| **도메인 전문성** | 각 에이전트가 1개 서비스 도메인의 비즈니스 규칙/상태머신/API를 깊이 이해 |
| **사람이 투입 결정** | 어떤 에이전트를 배치할지는 사람이 결정 — 에이전트 자동 판별 아님 |
| **같은 소스, 다른 렌즈** | 여러 에이전트가 동일 소스를 독립적으로 분석 — 소스 분배 문제 제거 |
| **기존 규칙 완전 보존** | v3.0의 R1~R6, A1~A5, 시트 분류, ISO/IEC 25010 매핑 그대로 유지 |

### 1.2 3-레이어 구조

```
┌──────────────────────────────────────────────────────┐
│                    사람 (운영자)                        │
│  - 프로젝트 소스 URL 설정                               │
│  - 투입 도메인 에이전트 선택                              │
│  - 매트릭스 검토 및 승인                                 │
└──────────────────┬───────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────┐
│              오케스트레이터 에이전트                       │
│  - 도메인별 결과 병합 (Merge Engine)                     │
│  - 크로스 도메인 분석 (Cross-Domain Analyzer)            │
│  - 갭 탐지 (Gap Detector)                              │
│  - Phase 2 실행 (TC 생성 총괄)                          │
└──────────────────┬───────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────┐
│              도메인 에이전트 (N개 병렬)                    │
│                                                       │
│  [펀딩 Agent]  [회원 Agent]  [메이커 Agent]  ...        │
│       │              │              │                  │
│  ┌────▼────┐   ┌────▼────┐   ┌────▼────┐             │
│  │서브에이전트│   │서브에이전트│   │서브에이전트│             │
│  └─────────┘   └─────────┘   └─────────┘             │
└──────────────────────────────────────────────────────┘
                   │
┌──────────────────▼───────────────────────────────────┐
│              공통 지식 레이어 (Common Knowledge)          │
│  - v3.0 프레임워크 규칙 (R1~R6, A1~A5)                  │
│  - 시트 분류 기준, ISO/IEC 25010 매핑                    │
│  - TC 포맷 규칙, 요약 파일 템플릿                         │
│  - ISTQB Advanced Level 학습 자료                       │
└──────────────────────────────────────────────────────┘
```

---

## 2. 도메인 에이전트

### 2.1 15개 도메인 목록

ServiceKMS 기반으로 식별된 와디즈 서비스 도메인입니다.

| # | 도메인 | Agent ID | ServiceKMS 참조 | 핵심 범위 |
|---|--------|----------|----------------|----------|
| 1 | 메인홈/전시 | `home-agent` | 2-01 | 메인 페이지, 배너, 전시 큐레이션 |
| 2 | 펀딩/프로젝트 | `funding-agent` | 2-02 | 프로젝트 생성/수정/심사, 리워드, 펀딩 상세 |
| 3 | 메이커 서비스 | `maker-agent` | 2-06 | 메이커홈, 메이커 대시보드, 서포터 관리 |
| 4 | 회원/인증 | `user-agent` | 2-03 | 회원가입, 로그인, 본인인증, 프로필 |
| 5 | 결제/정산 | `payment-agent` | 2-04 | 결제 수단, 환불, 정산, 수수료 |
| 6 | 알림 서비스 | `notification-agent` | 2-05 | 푸시, 이메일, 카카오 알림, 알림 설정 |
| 7 | 광고센터 | `ad-agent` | 8-1 | 광고 생성, 집행, 리포트, 과금 |
| 8 | 커뮤니티/소셜 | `community-agent` | 2-08 | 댓글, 새소식, 팔로우, 공유, 신고 |
| 9 | 통합검색 | `search-agent` | 2-07 | 검색 필터, 정렬, 자동완성, 검색 결과 |
| 10 | 위시/찜하기 | `wish-agent` | 2-09 | 찜 목록, 찜 알림, 오픈 알림 |
| 11 | 통합기획전 | `exhibition-agent` | 2-10 | 기획전 페이지, 참여 프로젝트, 큐레이션 |
| 12 | 글로벌 | `global-agent` | 2-11 | 다국어, 글로벌 펀딩, 환율, 배송 |
| 13 | CRM/마케팅 | `crm-agent` | 2-12 | 쿠폰, 포인트, 이벤트, 마케팅 자동화 |
| 14 | 어드민 | `admin-agent` | 9-xx | 관리자 콘솔, 심사, 운영 도구 |
| 15 | WAi (AI) | `wai-agent` | 신규 | AI 챗봇, 추천, 자동 분류 |

> **제외**: 투자/증권 서비스 (별도 법적 규제 영역)

### 2.2 도메인 지식 베이스 (DKB)

각 에이전트는 `_agents/{agent-id}/` 디렉토리에 도메인 전문 지식을 축적합니다.

```
_agents/funding-agent/
├── service_profile.md    # 서비스 개요, 핵심 기능, 사용자 페르소나
├── kms_index.md          # ServiceKMS 페이지 목록 + 요약
├── code_map.md           # 관련 코드 모듈/패키지 매핑
├── api_contracts.md      # API 엔드포인트, 요청/응답 스키마
├── business_rules.md     # 비즈니스 규칙, 정책, 제약 조건
├── state_machines.md     # 핵심 상태 머신 (프로젝트 상태, 결제 상태 등)
├── dependencies.md       # 의존하는/의존받는 다른 도메인 목록
└── historical_defects.md # 과거 주요 결함 패턴
```

### 2.3 인터페이스 계약 (크로스 도메인 연결)

각 에이전트는 다른 도메인과의 접점을 명시합니다.

```markdown
# dependencies.md 예시 (funding-agent)

## 의존하는 도메인 (Consumes)
| 도메인 | 접점 | 내용 |
|--------|------|------|
| user-agent | 로그인 상태 확인 | 비로그인 시 펀딩하기 차단 |
| payment-agent | 결제 API | 펀딩 결제 처리 |
| notification-agent | 알림 발송 | 펀딩 성공/실패 알림 |

## 의존받는 도메인 (Publishes)
| 도메인 | 접점 | 내용 |
|--------|------|------|
| maker-agent | 프로젝트 목록 API | 메이커홈에 프로젝트 노출 |
| home-agent | 프로젝트 카드 | 메인 전시에 펀딩 프로젝트 표시 |
```

---

## 3. 서브에이전트 아키텍처

### 3.1 설계 원칙: 단계별 최적 분할 축

| Phase | 분할 축 | 이유 |
|-------|---------|------|
| Phase 1 (소스분석) | **소스 타입별** | 입력 경계가 명확, 중복 없음, 병렬 효과 극대화 |
| Phase 1.5 (매트릭스) | **분할 없음** (도메인 에이전트 본체) | 교차 분석은 전체 소스를 종합해야 함 |
| Phase 2 (TC생성) | **비즈니스 기능별** | 좁은 범위 집중으로 TC 깊이 극대화 |

### 3.2 Phase 1 서브에이전트 (소스 타입별, 병렬 4개)

```
[도메인 에이전트] (예: funding-agent)
│
├── Phase 1: 소스 타입별 서브에이전트 (병렬)
│   ├── [Confluence 분석 서브에이전트]
│   │   → 프롬프트에 도메인 렌즈 주입
│   │   → "펀딩 프로젝트 생성, 리워드, 심사 관련 요소 중심으로 분석"
│   │   → 출력: _summaries/{agent-id}/confluence_summary.md
│   │
│   ├── [Figma 분석 서브에이전트]
│   │   → "펀딩 금액, 달성률, 리워드 선택 UI, 결제 CTA 중심으로 분석"
│   │   → 출력: _summaries/{agent-id}/figma_summary.md
│   │
│   ├── [Jira 분석 서브에이전트]
│   │   → "펀딩 관련 Epic/Task, PR 중심으로 분석"
│   │   → 출력: _summaries/{agent-id}/jira_summary.md
│   │
│   └── [Code 분석 서브에이전트]
│       → "펀딩 API 엔드포인트, 상태 관리, 에러 핸들링 중심으로 분석"
│       → 출력: _summaries/{agent-id}/code_summary.md
```

**핵심**: 각 서브에이전트는 부모 도메인 에이전트로부터 **도메인 렌즈 프롬프트**를 전달받아, 동일 소스에서도 해당 도메인에 관련된 요소만 집중 추출합니다.

### 3.3 Phase 2 서브에이전트 (비즈니스 기능별, 병렬)

Phase 1.5에서 생성된 매트릭스를 기능 영역별로 분배하여 TC를 생성합니다.

```
[도메인 에이전트] (예: funding-agent)
│
└── Phase 2: 비즈니스 기능별 서브에이전트 (병렬)
    ├── [Project 서브에이전트]
    │   ← 매트릭스에서 "프로젝트 생성/수정/심사" 관련 행 전달
    │   → 명세기반 + 예외사항 + 유즈케이스 TC 생성
    │
    ├── [Reward 서브에이전트]
    │   ← 매트릭스에서 "리워드 옵션/수량/조합" 관련 행 전달
    │   → 명세기반 + 예외사항 TC 생성
    │
    ├── [Campaign 서브에이전트]
    │   ← 매트릭스에서 "프리오픈/얼리버드/알림" 관련 행 전달
    │   → 명세기반 + 유즈케이스 TC 생성
    │
    └── [Settlement 서브에이전트]
        ← 매트릭스에서 "정산/성공실패 처리" 관련 행 전달
        → 코드영향도 + 예외사항 TC 생성
```

**분배 기준**: 매트릭스의 행 단위로 분배하므로, 소스 분배 문제가 발생하지 않습니다.
Phase 2 서브에이전트 구성은 도메인마다 다르며, DKB의 `service_profile.md`에 정의합니다.

### 3.4 전체 흐름 (1개 도메인 에이전트 내부)

```
[도메인 에이전트 시작]
│
├─ Phase 1: 소스 타입별 서브에이전트 4개 병렬 실행
│   ├── Confluence 서브에이전트 → confluence_summary.md
│   ├── Figma 서브에이전트     → figma_summary.md
│   ├── Jira 서브에이전트      → jira_summary.md
│   └── Code 서브에이전트      → code_summary.md
│
├─ Phase 1.5: 도메인 에이전트 본체가 직접 수행
│   ├── 4개 요약 파일 읽기
│   ├── 소스 간 교차 분석 (6쌍)     → cross_analysis.md
│   ├── 커버리지 매트릭스 생성       → coverage_matrix.md
│   └── 경계 보고서 생성            → boundary_report.md
│
├─ [사람 검토: 매트릭스 승인]
│
└─ Phase 2: 비즈니스 기능별 서브에이전트 N개 병렬 실행
    ├── 기능A 서브에이전트 → TC 세트 A
    ├── 기능B 서브에이전트 → TC 세트 B
    └── 기능C 서브에이전트 → TC 세트 C
    │
    └─ 도메인 에이전트 본체: TC 통합 + A1~A5 자기 점검
```

### 3.5 중첩 깊이 제한

```
오케스트레이터 (Level 0)
  └── 도메인 에이전트 (Level 1)
        └── 서브에이전트 (Level 2)  ← 최대 깊이
```

- **2단계까지만** 허용 (Level 0 → Level 1 → Level 2)
- 서브에이전트는 추가 하위 에이전트를 생성하지 않음
- 서브에이전트 간 결과 전달은 **파일 기반** (부모가 파일 읽기로 취합)

---

## 4. "같은 소스, 다른 렌즈" 원칙

### 4.1 동작 방식

여러 도메인 에이전트가 동일한 소스 URL을 독립적으로 분석합니다.

```
사람: "이번 프로젝트는 Funding, User 도메인 관련"
      + Figma URL 1개, Confluence URL 1개, Jira Epic 1개

[오케스트레이터]
  │
  ├── [Funding Agent]
  │   ├── Confluence 서브 → "펀딩 프로세스, 리워드 정책 중심 추출"
  │   ├── Figma 서브      → "펀딩 금액, 리워드 선택, 결제 버튼 중심 추출"
  │   ├── Jira 서브       → "펀딩 관련 Task, PR 중심 추출"
  │   └── Code 서브       → "펀딩 API, 결제 로직 중심 추출"
  │
  └── [User Agent]
      ├── Confluence 서브 → "로그인 정책, 인증 플로우 중심 추출"
      ├── Figma 서브      → "로그인 모달, 프로필 영역, 인증 분기 중심 추출"
      ├── Jira 서브       → "회원 관련 Task, PR 중심 추출"
      └── Code 서브       → "인증 미들웨어, 세션 관리 중심 추출"
```

### 4.2 이 방식의 장점

| 장점 | 설명 |
|------|------|
| **소스 분배 문제 제거** | "이 소스의 어느 부분을 누구에게?"라는 질문 자체가 사라짐 |
| **경계 영역 TC 자동 생성** | 양쪽 에이전트가 경계 화면을 각자 관점으로 TC 작성 → 누락보다 중복 발생 |
| **사람 개입 최소화** | "어떤 에이전트를 투입할지" 1가지만 결정하면 됨 |
| **병렬 실행** | 각 도메인 에이전트가 동시에 동일 소스 분석 → 시간 추가 없음 |

### 4.3 중복 TC 처리

오케스트레이터의 Merge Engine이 중복을 제거합니다:

```
[Funding Agent TC]
  "비로그인 상태에서 펀딩하기 클릭 시 로그인 유도 확인"

[User Agent TC]
  "펀딩 페이지에서 로그인 팝업 노출 후 로그인 성공 시 원래 페이지 복귀"

→ 관점이 다르므로 둘 다 유지 (중복 아님)

[Funding Agent TC]
  "펀딩 상세 페이지 프로젝트 제목 노출 확인"

[User Agent TC]
  "펀딩 상세 페이지 프로젝트 제목 노출 확인"

→ 동일 TC → Merge Engine이 1개로 통합
```

**중복 판정 기준**:
- 테스트제목 + 사전조건 + 기대결과가 실질적으로 동일 → 통합
- 동일 화면이지만 검증 포인트가 다름 → 둘 다 유지

---

## 5. 오케스트레이터 에이전트

### 5.1 4개 모듈

```
[오케스트레이터]
├── Merge Engine          # 도메인별 결과 병합 + 중복 제거
├── Cross-Domain Analyzer # 도메인 간 경계 TC 생성
├── Gap Detector          # 매트릭스 대비 누락 탐지
└── Phase 2 Executor      # TC 생성 총괄 + xlsx 출력
```

### 5.2 Merge Engine

각 도메인 에이전트의 산출물을 1개의 통합 매트릭스로 병합합니다.

**입력**: 각 도메인의 `_summaries/{agent-id}/coverage_matrix.md`
**출력**: `_summaries/merged_coverage_matrix.md`

병합 규칙:
1. 도메인별 매트릭스를 순차 적재
2. 조합 ID에 도메인 접두사 부여 (예: `FND-001`, `USR-001`)
3. 중복 행 탐지 및 통합 (테스트 차원 + 기대결과 기준)
4. 통합 카테고리 트리 재구성 (1차: 도메인, 2차~3차: 도메인 내부)

### 5.3 Cross-Domain Analyzer

각 도메인의 `boundary_report.md`를 수집하여 크로스 도메인 TC를 생성합니다.

```
[Funding Agent] boundary_report.md:
  "비로그인 펀딩 시도 → User 도메인 로그인 유도 필요"

[User Agent] boundary_report.md:
  "펀딩 페이지에서 로그인 복귀 시 원래 동작 재개 필요"

→ Cross-Domain TC:
  "비로그인 사용자가 펀딩하기 클릭 → 로그인 → 펀딩 상세 페이지로 복귀 → 결제 진행"
  (유즈케이스 시트 배치, 3+ 상태 전이)
```

### 5.4 Gap Detector

통합 매트릭스와 최종 TC를 1:1 대조하여 누락을 탐지합니다.

- 매트릭스 조합 행 중 TC가 없는 행 식별
- A1~A5 승수 대비 실제 TC 수 비교
- 크로스 도메인 경계 TC 존재 여부 확인

### 5.5 Phase 2 Executor

TC 생성을 총괄하고 최종 xlsx를 출력합니다.

- 각 도메인 에이전트의 Phase 2 결과를 수집
- 시트별 분류 (기준_시트분류.md 플로차트 적용)
- 케이스번호 체계 통합 (도메인 접두사 유지)
- 정보 시트 생성 (프로젝트 개요, 도메인별 TC 통계)
- xlsx 파일 출력

---

## 6. 3-레이어 지식 모델

### Layer 1: 공통 규칙 (Universal Rules)

모든 에이전트가 공유하는 프레임워크 규칙입니다. 기존 v3.0 파일 그대로 사용합니다.

| 파일 | 내용 |
|------|------|
| 규칙_TC포맷과소스.md | TC 포맷 8컬럼, 시트 6개, 비고란 형식 |
| 기준_시트분류.md | 5개 시트 분류 플로차트 + ISO/IEC 25010 매핑 |
| 기준_유저스토리.md | 3축 분류 (State × Situation × Action) |
| 기준_실행단위분해.md | A1~A5 실행 단위 분해 규칙 |
| 역할_QA전문가.md | QA 페르소나, ISTQB 기법 목록 |
| 가이드_워크플로우.md | 3-Phase 워크플로우, R1~R6, 요약 템플릿 |

### Layer 2: 공유 컴포넌트 (Shared Components)

여러 도메인에서 재사용되는 기능 패턴입니다.

```
_shared/
├── patterns/
│   ├── pagination.md       # 페이지네이션 TC 패턴
│   ├── search_filter.md    # 검색/필터 TC 패턴
│   ├── comment.md          # 댓글 CRUD TC 패턴
│   ├── share.md            # 공유하기 TC 패턴
│   ├── follow.md           # 팔로우/언팔로우 TC 패턴
│   ├── wish.md             # 찜하기 TC 패턴
│   ├── report.md           # 신고하기 TC 패턴
│   └── notification.md     # 알림 수신 TC 패턴
└── api_patterns/
    ├── auth_guard.md       # 인증 가드 TC 패턴
    ├── error_handling.md   # 공통 에러 핸들링 TC 패턴
    └── rate_limiting.md    # API 속도 제한 TC 패턴
```

각 패턴 파일은 해당 기능의 **표준 TC 세트**를 정의하며, 도메인 에이전트는 A2(컨텍스트 복제) 규칙에 따라 자기 도메인의 컨텍스트로 복제 적용합니다.

### Layer 3: 도메인 전용 지식 (Domain-Specific)

각 도메인 에이전트의 DKB입니다. (2.2절 참조)

**Override 규칙**: Layer 3(도메인)이 Layer 2(공유)를 override 가능
- 예: 펀딩 도메인의 댓글은 "서포터만 작성 가능" → 공유 패턴의 "모든 사용자" 규칙을 override

---

## 7. 워크플로우

### 7.1 신규 슬래시 커맨드

| 커맨드 | 설명 | 실행 주체 |
|--------|------|----------|
| `/팀배포` | 에이전트팀 초기 설정 (DKB 생성, 소스 설정) | 오케스트레이터 |
| `/팀소스분석` | 선택된 도메인 에이전트들이 Phase 1 병렬 실행 | 오케스트레이터 → 도메인 에이전트들 |
| `/팀매트릭스` | 각 도메인 Phase 1.5 + 오케스트레이터 병합 | 오케스트레이터 → 도메인 에이전트들 |
| `/팀TC생성` | 통합 매트릭스 기반 TC 생성 + xlsx 출력 | 오케스트레이터 → 도메인 에이전트들 |

### 7.2 기존 커맨드 호환

단일 도메인 프로젝트에서는 기존 v3.0 커맨드를 그대로 사용할 수 있습니다.

| 기존 커맨드 | 동작 |
|------------|------|
| `/소스분석` | 단일 에이전트 모드로 Phase 1 실행 (v3.0 호환) |
| `/매트릭스` | 단일 에이전트 모드로 Phase 1.5 실행 |
| `/TC생성` | 단일 에이전트 모드로 Phase 2 실행 |
| `/TC리뷰` | 단일 에이전트 모드로 리뷰 실행 |

### 7.3 전체 워크플로우 (멀티 도메인)

```
Step 1: 사람이 프로젝트 설정
  ├── 설정_소스경로.md에 소스 URL 입력
  └── 투입할 도메인 에이전트 선택 (예: funding, user, maker)

Step 2: /팀소스분석
  ├── 오케스트레이터가 선택된 3개 도메인 에이전트를 병렬 실행
  ├── 각 도메인 에이전트 내부에서 4개 소스 서브에이전트 병렬 실행
  │   (3 도메인 × 4 소스 = 12 서브에이전트 동시 실행)
  └── 결과: _summaries/{agent-id}/ 에 도메인별 4개 요약 파일 생성

Step 3: /팀매트릭스
  ├── 각 도메인 에이전트가 자체 교차 분석 + 매트릭스 생성
  ├── 오케스트레이터가 도메인별 매트릭스를 병합
  ├── Cross-Domain Analyzer가 경계 TC 식별
  └── 결과: _summaries/merged_coverage_matrix.md

Step 4: 사람이 통합 매트릭스 검토 및 승인

Step 5: /팀TC생성
  ├── 각 도메인 에이전트가 비즈니스 기능별 서브에이전트로 TC 생성
  ├── 오케스트레이터가 TC 병합 + 중복 제거 + 시트 분류
  ├── Gap Detector가 누락 확인
  └── 결과: {프로젝트명}_테스트케이스.xlsx
```

---

## 8. 디렉토리 구조

```
{프로젝트명}/
├── CLAUDE.md                       # 프로젝트 설정 + 투입 에이전트 목록
├── 설정_소스경로.md                  # 소스 URL + 로컬 경로
│
├── [기존 v3.0 공통 규칙 파일들]
│   ├── 규칙_TC포맷과소스.md
│   ├── 기준_시트분류.md
│   ├── 기준_유저스토리.md
│   ├── 기준_실행단위분해.md
│   ├── 역할_QA전문가.md
│   ├── 가이드_워크플로우.md
│   └── 가이드_클로드코드활용.md
│
├── _shared/                        # [신규] Layer 2 공유 패턴
│   ├── patterns/
│   │   ├── pagination.md
│   │   ├── comment.md
│   │   └── ...
│   └── api_patterns/
│       ├── auth_guard.md
│       └── ...
│
├── _agents/                        # [신규] Layer 3 도메인 DKB
│   ├── funding-agent/
│   │   ├── service_profile.md
│   │   ├── kms_index.md
│   │   ├── code_map.md
│   │   ├── api_contracts.md
│   │   ├── business_rules.md
│   │   ├── state_machines.md
│   │   ├── dependencies.md
│   │   └── historical_defects.md
│   ├── user-agent/
│   │   └── ...
│   └── maker-agent/
│       └── ...
│
├── _summaries/                     # Phase 1 + 1.5 결과물
│   ├── funding-agent/              # 도메인별 분리
│   │   ├── confluence_summary.md
│   │   ├── figma_summary.md
│   │   ├── jira_summary.md
│   │   ├── code_summary.md
│   │   ├── cross_analysis.md
│   │   ├── coverage_matrix.md
│   │   └── boundary_report.md
│   ├── user-agent/
│   │   └── ...
│   ├── merged_coverage_matrix.md   # 오케스트레이터 병합 결과
│   └── cross_domain_analysis.md    # 크로스 도메인 분석
│
├── .claude/
│   └── commands/
│       ├── 소스분석.md              # 기존 (v3.0 호환)
│       ├── 매트릭스.md              # 기존 (v3.0 호환)
│       ├── TC생성.md                # 기존 (v3.0 호환)
│       ├── TC리뷰.md               # 기존 (v3.0 호환)
│       ├── 팀배포.md               # [신규] 에이전트팀 초기 설정
│       ├── 팀소스분석.md            # [신규] 멀티 도메인 Phase 1
│       ├── 팀매트릭스.md            # [신규] 멀티 도메인 Phase 1.5
│       └── 팀TC생성.md             # [신규] 멀티 도메인 Phase 2
│
├── 설계_에이전트팀_아키텍처.md       # 이 문서
├── 이력_프레임워크변경.md
└── {프로젝트명}_테스트케이스.xlsx    # 최종 산출물
```

---

## 9. CLAUDE.md 확장 포맷

에이전트팀 모드에서 CLAUDE.md에 추가되는 섹션입니다.

```markdown
## 에이전트팀 설정

### 투입 도메인 에이전트
| Agent ID | 도메인 | 투입 사유 |
|----------|--------|----------|
| funding-agent | 펀딩/프로젝트 | 메이커홈에서 펀딩 프로젝트 목록/카드 표시 |
| user-agent | 회원/인증 | 로그인 상태별 UI 분기, 프로필 표시 |
| maker-agent | 메이커 서비스 | 메이커홈 본체 기능 |

### Phase 2 서브에이전트 구성
#### funding-agent
- Project: 프로젝트 생성, 수정, 심사 제출
- Reward: 리워드 옵션 설정, 수량 관리, 조합
- Campaign: 프리오픈, 얼리버드, 알림 신청
- Settlement: 펀딩 마감 후 정산, 성공/실패 처리

#### user-agent
- Auth: 로그인, 회원가입, 소셜 로그인
- Profile: 프로필 조회/수정, 프로필 이미지
- Verification: 본인인증, 성인인증

#### maker-agent
- Dashboard: 메이커 대시보드, 통계
- Supporter: 서포터 관리, 문의 응대
- Settings: 메이커 설정, 프로필 관리
```

---

## 10. 구현 로드맵

### Phase A: 기반 구조 (즉시)
- [ ] 디렉토리 구조 생성 (`_agents/`, `_shared/`)
- [ ] 신규 슬래시 커맨드 4종 생성
- [ ] CLAUDE.md 확장 포맷 정의

### Phase B: 파일럿 (1개 도메인)
- [ ] 메이커 서비스 에이전트 DKB 구축 (현재 프로젝트 활용)
- [ ] 단일 도메인으로 전체 워크플로우 검증
- [ ] v3.0 대비 TC 수량/깊이 비교

### Phase C: 멀티 도메인 (2~3개)
- [ ] 메이커홈 개편에 funding + user + maker 3개 에이전트 투입
- [ ] 오케스트레이터 Merge Engine 검증
- [ ] Cross-Domain Analyzer 검증

### Phase D: 공유 패턴 라이브러리
- [ ] Phase B~C에서 반복 발견되는 패턴을 `_shared/patterns/`로 추출
- [ ] 패턴 재사용에 의한 TC 생성 효율 측정

### Phase E: 전체 도메인 확장
- [ ] 나머지 도메인 에이전트 DKB 순차 구축
- [ ] ServiceKMS 연동 자동화 (kms_index.md 자동 갱신)

---

## 11. 성공 지표

| 지표 | v3.0 (단일) | v4.0 (에이전트팀) 목표 |
|------|------------|----------------------|
| TC 수량 (동일 범위 기준) | 370개 | ×2~3 배 |
| 소스 분석 시간 | 순차 4회 | 병렬 (1/3~1/4 시간) |
| 크로스 도메인 TC | 수동 식별 | 자동 식별 + 생성 |
| 도메인 엣지 케이스 | 범용 분석 | DKB 기반 심층 분석 |
| 매트릭스 커버리지 | 100% | 100% (도메인별 + 크로스) |
| AI 임의 중단 | 미발생 | 미발생 유지 |

---

## 부록: 아키텍처 결정 기록 (ADR)

### ADR-01: 소스 분배를 자동화하지 않는 이유
- **결정**: 소스 URL을 도메인별로 분배하지 않고, 모든 에이전트가 동일 소스를 독립 분석
- **이유**: Figma/Confluence 1개 페이지에 여러 도메인 컨텐츠가 혼재. 자동 분류의 정확도가 도메인 지식에 의존하므로 사전에 보장 불가
- **결과**: 소스 분배 문제 제거. 약간의 중복 발생 → Merge Engine에서 제거

### ADR-02: Phase별 서브에이전트 분할 축이 다른 이유
- **결정**: Phase 1은 소스 타입별, Phase 2는 비즈니스 기능별 서브에이전트
- **이유**: Phase 1의 입력은 소스 타입으로 경계가 명확 (Confluence는 Confluence만 읽으면 됨). Phase 2의 입력은 매트릭스 행 단위로 기능별 분배가 자연스러움
- **대안 검토**: 전 Phase 비즈니스 기능별 분할 → 소스 분배 문제 재발 + 4×N 중복 읽기 비효율

### ADR-03: 사람이 에이전트 투입을 결정하는 이유
- **결정**: 오케스트레이터가 자동 판별하지 않고, 사람이 투입 에이전트를 지정
- **이유**: 프로젝트 범위와 도메인 경계는 조직적/비즈니스적 판단. AI보다 사람이 정확
- **결과**: 사람의 의사결정 포인트가 명확 — "어떤 에이전트를 투입할지" 1가지만 결정

### ADR-04: 중첩 깊이를 2단계로 제한하는 이유
- **결정**: 오케스트레이터 → 도메인 에이전트 → 서브에이전트 (최대 3단계)
- **이유**: 3단계 이상은 컨텍스트 전달 복잡도 증가 + 디버깅 어려움 + 비용 급증
- **대안**: 서브에이전트 내 추가 분할 → 비용 대비 효과 불명확
