# 테스트케이스 작성 워크플로우

컨텍스트 윈도우 최적화를 위한 단계별 테스트케이스 작성 방법론

## 개요

대규모 명세(Confluence, Jira, Figma, Code 등)를 기반으로 테스트케이스를 작성할 때, 모든 소스를 한 번에 분석하면 컨텍스트 제한에 걸릴 수 있습니다. 이를 해결하기 위해 **분석 단계를 분리**하고 **소스별 요약을 생성**하는 방식을 사용합니다.

### 3가지 구조적 문제와 해결 방향

AI로 TC를 생성할 때 다음 3가지 문제가 발생합니다:

1. **AI의 완결성 판단 편향**: 기능별 대표 표본 1~3개만 생성하고 조합을 펼치지 않음
2. **요약 압축에 의한 정보 손실**: 서술형 요약만으로는 세부 분기/엣지 케이스가 탈락
3. **출력 토큰 한계**: 6개 시트를 한 번에 생성하면 구조적 오버헤드로 토큰 소비

**해결 방향**: AI는 무엇을 생략했는지 알고 있으므로, "빠뜨린 것을 먼저 식별하라"는 프로세스(커버리지 매트릭스)를 넣어 외부 기준점 역할을 하게 합니다. 또한 시트별 분할 생성으로 토큰 한계를 우회합니다.

## 워크플로우 (3-Phase)

```
Phase 1 (소스분석)     Phase 1.5 (매트릭스)     Phase 2 (TC생성)
   [소스별 요약]          [커버리지 기준점]        [시트별 분할 생성]

소스 4종 분석     →   교차분석+매트릭스   →   시트별 분할 생성
  ↓                      ↓                      ↓
_summaries/          cross_analysis.md        xlsx 시트별 순차 생성
  4개 요약 파일      coverage_matrix.md       + 매트릭스 대비 검증
```

사용자 조작: `/소스분석 all` → `/매트릭스` → (매트릭스 확인) → `/TC생성 v1`

### Phase 1: 소스별 분석 및 요약 생성

각 소스를 분석하여 `_summaries/` 폴더에 요약을 생성합니다.
슬래시 커맨드 `/소스분석`으로 실행할 수 있습니다.

#### 1-1. Confluence 분석 (기획 문서)
```
/소스분석 confluence
→ MCP로 위키 페이지 분석 → _summaries/confluence_summary.md 생성
```

#### 1-2. Jira 분석 (개발 이슈)
```
/소스분석 jira
→ MCP로 에픽/태스크 분석 → _summaries/jira_summary.md 생성
```

#### 1-3. Figma 분석 (UI/UX 명세)
```
/소스분석 figma
→ MCP로 디자인 분석 → _summaries/figma_summary.md 생성
```

#### 1-4. 코드 분석
```
/소스분석 code
→ MCP + 로컬 코드 분석 → _summaries/code_summary.md 생성
```

#### 1-5. 전체 병렬 분석
```
/소스분석 all
→ 서브에이전트 4개가 동시에 분석 (가장 빠름)
```

#### 1-6. PDF 분석 (기존 TC 아카이브가 있는 경우)
```
/소스분석 pdf
→ TChive 폴더의 PDF 분석 → _summaries/pdf_summary.md 생성
```

### Phase 1.5: 소스 교차 분석 + 커버리지 매트릭스 생성

Phase 1.5는 두 가지를 수행합니다:
1. **소스 간 교차 분석**: 4개 소스를 6쌍으로 비교하여 불일치·누락 식별
2. **커버리지 매트릭스 생성**: 모든 조합을 열거하여 외부 기준점 생성

슬래시 커맨드 `/매트릭스`로 실행할 수 있습니다.

```
/매트릭스
→ _summaries/ 요약 4종 읽기
→ 소스 간 교차 분석 (6쌍 비교) → _summaries/cross_analysis.md 생성
→ 기능영역별 테스트 차원 식별 (교차 분석 발견 포함)
→ 차원 간 조합 테이블 생성
→ 시트 매핑 + 예상 TC 수 산출
→ _summaries/coverage_matrix.md 생성
```

#### 소스 간 교차 분석
4개 소스를 6쌍으로 교차 비교하여 **개별 소스만 보면 발견할 수 없는 TC**를 확보합니다:

| 비교 쌍 | 주요 비교 관점 |
|---------|---------------|
| Confluence ↔ Figma | 기획 요구사항이 UI에 반영되었는가? |
| Confluence ↔ Code | 기획 분기/로직이 코드에 구현되었는가? |
| Confluence ↔ Jira | 기획 변경이 개발 이슈에 반영되었는가? |
| Figma ↔ Code | UI 컴포넌트가 코드에 구현되었는가? |
| Figma ↔ Jira | 디자인 변경이 개발 범위에 포함되었는가? |
| Jira ↔ Code | 완료 Task가 실제 코드에 반영되었는가? |

**핵심 원칙: 교차 분석은 순수 추가(additive)만 한다**
- 각 소스에서 개별적으로 도출된 TC는 그대로 유지
- 교차 분석 발견 항목은 **추가 TC로만** 반영하며, 기존 TC를 요약·축소·통합하는 근거로 사용 금지
- 교차 분석 TC는 별도 시트를 만들지 않고, 성격에 맞는 기존 시트(명세기반, 예외사항 등)에 분배

#### 매트릭스가 AI 임의 중단을 방지하는 원리
- 각 영역에 **수치 목표**가 있으므로 대표 표본만 쓰고 멈출 수 없음
- 조합 테이블의 각 행이 "이 TC가 있어야 한다"는 **존재 증명**
- TC 생성 후 매트릭스 대비 누락 점검을 **강제**

#### 매트릭스 생성 규칙
1. 요약 파일의 "테스트 차원 열거" 섹션을 우선 참조
2. **입력값**, **분기 조건**, **상태 전이**, **사용자/플랫폼 조합**을 빠짐없이 식별
3. 교차 분석 발견 항목을 **추가 테스트 차원**으로 포함
4. 조합 축소 규칙: 플랫폼/언어는 해당 기능에서만 분리, Each-choice + 리스크 기반 추가
5. 각 조합에 대상 시트(6개 중)와 ISTQB 기법을 매핑
6. 자기 점검 체크리스트로 완전성 검증 (교차 분석 반영 여부 포함)

#### 사용자 확인
매트릭스 생성 후 반드시 사용자에게 검토를 요청합니다. 사용자가 영역/차원을 조정할 수 있으며, 승인 후 Phase 2로 진행합니다.

### Phase 2: 테스트케이스 작성 (시트별 분할 생성)

커버리지 매트릭스를 기준으로 **시트별 분할 생성** 방식으로 TC를 작성합니다.
슬래시 커맨드 `/TC생성`으로 실행할 수 있습니다.

```
/TC생성 v1
→ coverage_matrix.md 존재 확인
→ 정보 시트 생성 (매트릭스 통계 반영)
→ 시트별 순차 생성 (매트릭스 매핑 항목 기반)
→ 통합 검증 (매트릭스 대비 커버리지율)
→ xlsx 파일 생성
```

#### 시트별 분할 생성 프로토콜
각 시트(명세기반, 예외사항, 유즈케이스, 코드영향도, Regression)마다:
1. 매트릭스에서 해당 시트에 매핑된 항목만 추출
2. "이 시트에서 최소 {N}개 TC 필요" 수치 목표 확인
3. 각 매핑 항목에 대해 TC 1개 이상 작성
4. 자기 점검: 매핑 항목 대비 누락이 있으면 추가 작성
5. 완료 후 다음 시트로 이동

#### 개별 시트 생성 옵션
```
/TC생성 시트:명세기반
→ 명세기반 시트만 개별 생성 (컨텍스트 절약)
```

## 요약 파일 템플릿

### confluence_summary.md
```markdown
# Confluence 분석 요약

## 문서 정보
- URL: [링크]
- 최종 수정일: [날짜]

## 기능 요구사항
1. [요구사항]

## 비즈니스 로직
- [로직 설명]

## 예외 케이스
- [예외]

## 테스트 차원 열거 (빠짐없이 나열)

### 입력값 열거
| 필드명 | 유효값 목록 | 무효값 목록 | 경계값 |
|--------|------------|------------|--------|

### 분기 조건 열거
| 조건 | 가능한 값들 |
|------|------------|

### 상태 열거
| 상태명 | 전이 가능 상태 | 전이 조건 |
|--------|---------------|-----------|
```

### jira_summary.md
```markdown
# Jira 분석 요약

## 분석 이슈
| 이슈 키 | 제목 | 유형 | 상태 |
|---------|------|------|------|

## 구현 범위
- [범위]

## 주요 변경 사항
1. [변경]

## 영향 범위
- [영향 받는 모듈/기능]

## 테스트 차원 열거 (빠짐없이 나열)

### 입력값 열거
| 필드명 | 유효값 목록 | 무효값 목록 | 경계값 |
|--------|------------|------------|--------|

### 분기 조건 열거
| 조건 | 가능한 값들 |
|------|------------|

### 상태 열거
| 상태명 | 전이 가능 상태 | 전이 조건 |
|--------|---------------|-----------|
```

### figma_summary.md
```markdown
# Figma 분석 요약

## UI 컴포넌트
| 컴포넌트 | 상태 | 설명 |
|----------|------|------|

## 사용자 인터랙션
1. [인터랙션]

## 입력 필드 및 검증
| 필드명 | 타입 | 필수여부 | 검증규칙 |
|--------|------|----------|----------|

## 플로우 분기
- [분기 조건]

## 테스트 차원 열거 (빠짐없이 나열)

### 입력값 열거
| 필드명 | 유효값 목록 | 무효값 목록 | 경계값 |
|--------|------------|------------|--------|

### 분기 조건 열거
| 조건 | 가능한 값들 |
|------|------------|

### 상태 열거
| 상태명 | 전이 가능 상태 | 전이 조건 |
|--------|---------------|-----------|
```

### code_summary.md
```markdown
# 코드 분석 요약

## 분석 대상
- [PR/브랜치]

## 주요 변경 파일
| 파일 | 설명 |
|------|------|

## 테스트 영향도
| 테스트 항목 | 검증 포인트 |
|-------------|-------------|

## 테스트 차원 열거 (빠짐없이 나열)

### 입력값 열거
| 필드명 | 유효값 목록 | 무효값 목록 | 경계값 |
|--------|------------|------------|--------|

### 분기 조건 열거
| 조건 | 가능한 값들 |
|------|------------|

### 상태 열거
| 상태명 | 전이 가능 상태 | 전이 조건 |
|--------|---------------|-----------|
```

## 장점

1. **컨텍스트 효율화**: 각 대화에서 필요한 소스만 로드
2. **재사용성**: 요약 파일을 다른 대화에서 재활용 가능
3. **추적성**: 어떤 소스에서 어떤 TC가 도출되었는지 추적 가능
4. **점진적 작업**: 일부 소스만 변경 시 해당 요약만 업데이트
5. **커버리지 측정**: 매트릭스 대비 TC 커버리지율을 정량적으로 산출 가능
6. **AI 임의 중단 방지**: 수치 목표가 있어 대표 표본만 쓰고 멈출 수 없음

## 주의사항

- 요약 생성 시 **테스트 관점**에서 중요한 정보 위주로 추출
- 서술형은 간결하게, **열거형 항목(입력값, 분기, 상태)은 빠짐없이 나열**
- 요약이 너무 길어지면 Phase 2에서도 컨텍스트 문제 발생 가능하므로, 서술은 압축하되 열거는 유지
- 매트릭스 생성 후 반드시 사용자 확인을 거쳐야 Phase 2 진행 가능

## 정확성 보장 규칙 (전체 Phase 공통)

Phase 1.5(매트릭스)와 Phase 2(TC생성)에서 AI 생성 오류를 방지하기 위한 필수 규칙입니다.
상세 내용은 `/매트릭스` 커맨드의 "정확성 보장 규칙" 섹션을 참조합니다.

| 규칙 | 핵심 내용 | 적용 Phase |
|------|-----------|-----------|
| **R1. 소스 파일 참조 필수** | 항목 작성 전 반드시 요약 파일을 Read로 열어 원문 확인. 기억 기반 작성 금지 | 1.5, 2 |
| **R2. 모순 검증** | 새 항목 추가 시 같은 소스 내 반대 기술 존재 여부 확인. 서로 다른 컨텍스트 혼동 방지 | 1.5, 2 |
| **R3. 원문 표현 유지** | 기술 용어·기능명·열거형 값·필드명은 소스 원문 그대로 사용. 동의어 치환 금지 | 1.5, 2 |
| **R4. 열거 완전성 검증** | "N종/N개" 수량 명시 항목은 개별 1:1 나열 후 수량 일치 확인 | 1, 1.5, 2 |
| **R5. 하위 항목 전개** | 2+ 하위 조건/상태/값이 있으면 각각 별도 행으로 전개. 대표 1개만 작성 금지 | 1.5, 2 |

### 규칙 위반 시 발생하는 오류 유형 (참고)
- R1 위반 → **내용 날조** (존재하지 않는 기능명/값을 작성)
- R2 위반 → **컨텍스트 혼동** (서로 다른 맥락의 기능을 잘못 기술)
- R3 위반 → **의미 변형** (분류 기준이 달라짐, 기대결과가 소스와 불일치)
- R4 위반 → **열거 누락** (N종 중 일부만 나열, 수량 불일치 미인지)
- R5 위반 → **대표 표본 편향** (여러 하위 유형 중 1개만 TC화)
